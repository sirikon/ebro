name: Build Ebro
on: [push]
jobs:
  build:
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build
        run: ./meta/docker/build.sh
      - name: Release
        if: startsWith(github.ref, 'refs/tags/')
        run: ./meta/release/github.sh
