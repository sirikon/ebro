tasks:
  something:
    script: |
      echo something

modules:
  config:
    for_each: |
      cd "${EBRO_ROOT}/config" && ls .

    tasks:
      default:
        requires: [echo]

      echo:
        script: |
          cat "${EBRO_ROOT}/config/${EBRO_EACH}/msg"
